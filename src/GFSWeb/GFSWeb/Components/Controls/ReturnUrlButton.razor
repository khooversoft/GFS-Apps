@if (ReturnUrl.IsNotEmpty())
{
    <MudButton Variant="Variant.Outlined" Color="Color.Info" Href=@ReturnUrl StartIcon="@Icons.Material.Outlined.ArrowBack" Style="max-width: 90px">
        Back
    </MudButton>
}

@code {
    [Parameter] public string ReturnUrl { get; set; } = null!;
}
